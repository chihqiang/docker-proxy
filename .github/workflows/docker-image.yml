name: docker-image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      # - name: Build and Push v2ray Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./v2ray
      #     push: true
      #     platforms: linux/amd64,linux/arm64
      #     tags: zhiqiangwang/proxy:v2ray

      # - name: Build and Push xray Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #       context: ./xray
      #       push: true
      #       platforms: linux/amd64,linux/arm64
      #       tags: zhiqiangwang/proxy:xray

      # - name: Build and Push openvpn Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #       context: ./openvpn
      #       push: true
      #       platforms: linux/amd64,linux/arm64
      #       tags: zhiqiangwang/proxy:openvpn
      
      # - name: Build and Push squid Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #       context: ./squid
      #       push: true
      #       platforms: linux/amd64,linux/arm64
      #       tags: zhiqiangwang/proxy:squid

      # - name: Build and Push dante Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #       context: ./dante
      #       push: true
      #       platforms: linux/amd64,linux/arm64
      #       tags: zhiqiangwang/proxy:dante

      - name: Build and Push ssh Docker image
        uses: docker/build-push-action@v5
        with:
            context: ./ssh
            push: true
            platforms: linux/amd64,linux/arm64
            tags: zhiqiangwang/proxy:ssh

